package Logic;

import FileIO.FileManager;
import Render.RenderManager;

public class Core
{
    final int WINDOW_WIDTH  = 0;
    final int WINDOW_HEIGHT = 0;

    public boolean initCore()
    {
        // -- set swing window --
        return true;
    }

    public boolean run()
    {
        // -- get line --
        // -- get clue --
        // -- check clue, log, line status --

        return true;
    }

    public void end()
    {
        // -- ending credit --
    }
}
